import { PhaseStub } from '@/routes/PhaseStub'

export default function Tasks() {
  return (
    <PhaseStub
      title="Tasks"
      phase="P2"
      what="Kanban with dnd-kit, status / assignee / due date, My Tasks, and create-task-from-message."
    />
  )
}
