import { PhaseStub } from '@/routes/PhaseStub'

export default function Channels() {
  return (
    <PhaseStub
      title="Channels"
      phase="P1"
      what="Realtime chat — optimistic send, threaded replies, uploads, @mentions, unread badges, reconnect resync."
    />
  )
}
