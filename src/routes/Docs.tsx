import { PhaseStub } from '@/routes/PhaseStub'

export default function Docs() {
  return (
    <PhaseStub
      title="Docs"
      phase="P4"
      what="Collections tree, BlockNote pages with 1s autosave, soft edit-lock banner, linked-items backlink panel."
    />
  )
}
